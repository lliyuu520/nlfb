# 上线验收清单（提审前人工执行项）

> 2026-09-07 依据《第一版上线门槛清单》分阶段实施后整理；2026-09-09 按 V1 收口清单二次代码走查并补修。
> 代码侧可机器完成的项已做完（见文末"已完成"），本清单只列**必须由人执行**的验收项。
> 每项验收完成后在 `[ ]` 打勾并记日期；全部 P0 打勾才提审。

## 0. 提审前代码开关（需要拍板）

- [x] **广告形态决策**（2026-09-07 已定）：按方案 b 实施——首版 `ADS_OFF=true` 隐藏全部广告入口（复活/结算翻倍/商店广告刷新/商店金币翻倍/设置页领金币及全部广告文案）；流量主开通后改 `js/main.js` 的 `ADS_OFF=false` 一处恢复。`USE_FAKE` 保持 true（恢复前不触发）。
- [x] `version.json` 与 `main.js` 的 `VERSION` 同步升版（2026-09-09 → 0.2.4）。
- [x] 调试开关确认全关：`DBG_AID=false`、`DBG_GOD=false`（2026-09-09 复扫，仍为 false）。

## 1. 真机稳定性（P0-1 / P0-5）

- [ ] 低端安卓机（2~3 年前机型）完整玩 3 局，观察掉帧/发热/闪退
- [ ] iPhone 各一台（有刘海/无刘海各一），HUD 不顶摄像头区、底部不压手势条
- [ ] 高频弹幕场景：第 3 关以后 + 狂暴 Boss 战，帧率肉眼可接受
- [ ] 连续运行 30 分钟不死机、不内存溢出（对象数量峰值场景）
- [ ] 频繁切后台/恢复 10 次：自动暂停正常、恢复后不花屏、数据不丢

## 2. 正式环境完整流程（P0-1）

体验版（正式 AppID）跑通，开发者工具通过不算数：

- [ ] 冷启动 → 首页 → 开始游戏
- [ ] 好友榜（开放数据域，真机才有效）
- [ ] 世界榜：上报 → 榜单展示 → 昵称修改；服务端域名 `https://game.lliyuu520.cn` 已加入 request 合法域名
- [ ] 断网开游戏（应可离线玩，榜单静默失败不卡流程）
- [ ] 弱网下游戏结束上报失败，不弹错误、下次破纪录补报
- [ ] 杀进程重开：最高分/金币/皮肤/背景/强化等级全部还在
- [ ] 微信后台「运维中心 → 错误查询」观察 1~2 天有无 JS 异常聚集（平台自动收集，无需埋点）

## 3. 广告真实环境（P0-2，恢复广告入口后执行；首版 ADS_OFF 全部隐藏，本节挂起）

- [ ] 完整观看 → 奖励到账 → 游戏状态正确（复活半血/金币入账/商店刷新）
- [ ] 连续看 3 次广告，每次奖励只发一次
- [ ] 中途退出广告 → 不发奖 + toast 提示
- [ ] 广告拉取失败（飞行模式）→ toast「广告暂不可用」，不卡死、不误发奖
- [ ] 服务端激励视频回调（流量主后台配置）如接入，需验证防重放；未接入则客户端幂等守卫为准（已审计）

## 4. 数值通关测试（P1-8）

- [ ] 第一次玩（零基础）10 秒内明白操作（新手引导 8 秒提示，可在模拟器清 `nulei_tut` 复看）
- [ ] 第 1 关 Boss 能被打到但不轻松过；第 1 关弹速/密度对新手温和
- [ ] 前 3 关前几分钟"爽"，第 4 关起压力爬升
- [ ] 金币经济：一局总收入 vs 商店定价，第 2 关起能买到 1~2 张牌（构筑正反馈成立）

## 5. 提审材料

- [ ] 小游戏备案已通过（2026-09-05 提交，约 10 个工作日；备案期间不改名/不迁主体）
- [ ] 类目、隐私协议、用户协议齐全
- [ ] 体验版二维码 + 测试账号（如需）交给审核备注

---

## 已完成（2026-09-07 第一轮实施，代码侧）

| 清单项 | 状态 |
|--------|------|
| P0-4 排行榜安全 | 服务端新增：单局时长上限 `maxRunSec`(3600s)、玩家累计上报时长 ≤ 墙钟+1h 预算（重启持久化）；原有分数硬上限/速率校验/每日次数/IP 限额保留。devLogin 实例 11 组 curl 用例全过 |
| P0-3 数据持久化 | 存档点审计通过（hi/coins/ship/bg/owned/up 全有写档）；全部 storage 读写加 try/catch 容错，存储异常不再带崩冷启动/整局 |
| P0-2 广告幂等 | 审计通过：`reviving` 并发守卫 + `settleDoubled/coinDoubled/adRefreshUsed` 单次标记 + `ok===true` 才发奖 + 失败/取消 toast，无需改码 |
| P1-6 新手引导 | 已实现：首次游戏开局 8 秒双行提示，看满写档 `nulei_tut` 永不再出 |
| P1-7 结算闭环 | 审计达标：标题→游戏→双击暂停(继续/重开/回标题)→Game Over→再来一局/复活/翻倍，onHide 自动暂停 |
| P1-9 首页克制 | 审计达标：仅 开始/排行/设置/声音 四入口 |
| P0-5 崩溃防护 | `DBG_AID/DBG_GOD` 已置 false（原为 true，属上线硬伤）；JS 错误由微信平台自动收集 |
| P2 肉鸽等 | 未动；`docs/roguelike-0.1.0-plan.md` 挂起到上线后决策 |

## 已完成（2026-09-09 第二轮代码走查）

| 清单项 | 状态 |
|--------|------|
| 正式包密钥/杂项 | `project.config.json` packOptions 补齐 ignore：`.env`（阿里云 OSS Key）、`.idea`、`.zcode`、`__pycache__`、死代码 `js/loop.js`（仍引用 `performance.now`，入口未 require，但不应进包） |
| 存档容错补漏 | `sfx.js` 模块顶层 `getStorageSync` 原先无 try/catch，与 main 的 `lsGet` 不一致；已统一容错，存储异常不再带崩冷启动 |
| 局内金币结算 | `convertLeftoverCoins` 增加 `runSettled` 幂等标记；死亡结算与回标题二次折算风险消除。复活窗口内（`ADS_OFF=false` 时）先不折算，避免「死亡入账→复活继续用同一笔局内金币→再死再入账」的双花；V1 `ADS_OFF=true` 行为不变（死亡即结算） |
| 文案对齐 | 「纳米装甲」描述由旧命数制 `生命上限+1` 改为血条制 `生命上限+25 并立即回复25`（与 `applyBuff` 实际效果一致） |
| 好友榜真机空白 | 未提交改动保留：主域每 600ms `redraw` 保活 + 子域缓存先上屏、失败不清缓存（防 sharedCanvas 被系统回收） |
| 广告入口复扫 | `ADS_OFF=true` 下触控与绘制两侧均无广告入口；`requestRevive/settleDouble/shopAd/coinAd` 全部前置守卫；`USE_FAKE` 模拟层无会话时 `draw` 直接 return |
| 服务端 | `data/`、`bin/` 已在 `.gitignore`；本地 `devLogin=true` 仅本机配置，线上部署需确认为 `false`（运维项，见部署说明） |

### 仍须人工真机执行（代码改不了）

- 第 1 节真机稳定性、第 2 节正式环境完整流程、第 4 节数值通关、第 5 节提审材料
- 好友榜保活改动需真机验证：进好友榜 → 切世界榜再切回 → 列表仍可见不空白
- 体验版打包上传前，在微信开发者工具确认「代码包大小」与 ignore 是否生效（`.env` 不应出现在包内）

### 记录不处理（V2 / 非阻塞）

- Game Over 界面无「回到标题」入口（须先再来一局再暂停返回）——P2 体验项
- `clear` 通关动画期间敌机冻结在屏上——P2 视觉项
- `js/loop.js` 死代码可删，已从正式包 ignore，仓库暂留
- `USE_FAKE=true` / 占位 `adunit`：恢复广告前再改，与 V1 无关
